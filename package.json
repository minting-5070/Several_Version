{
  "name": "several-version",
  "version": "1.0.0",
  "description": "Multiple versions of chat applications",
  "private": true,
  "scripts": {
    "build": "cd ./SCI\\ 버전 && npm run build",
    "dev": "cd ./SCI\\ 버전 && npm run dev",
    "start": "cd ./SCI\\ 버전 && npm run start",
    "install": "cd ./SCI\\ 버전 && npm ci"
  },
  "workspaces": [
    "chat-version",
    "chat-version-API",
    "SCI 버전"
  ]
}
