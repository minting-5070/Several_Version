{
  "name": "several-version",
  "version": "1.0.0",
  "description": "Multiple versions of chat applications",
  "private": true,
  "scripts": {
    "build": "cd ./sci-version && npm run build",
    "dev": "cd ./sci-version && npm run dev",
    "start": "cd ./sci-version && npm run start",
    "install": "cd ./sci-version && npm install"
  },
  "workspaces": [
    "chat-version",
    "chat-version-API",
    "sci-version"
  ]
}
